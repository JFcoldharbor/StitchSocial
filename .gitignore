*.iml
.gradle
/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties

app/release/*.apk
# Release bundles too. A 146MB AAB in git exceeds GitHub's 100MB limit and
# blocks the push outright, so this is not just tidiness.
app/release/*.aab

# Claude Code local state — settings, and worktrees under .claude/worktrees
# (an embedded git repo; committing it adds a stray gitlink).
.claude/

# Kotlin compiler error dumps — build noise, regenerated on every failure.
.kotlin/
